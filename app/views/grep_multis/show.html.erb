<p id="notice"><%= notice %></p>

<p>
  <strong>Search terms:</strong>
  <pre><%= @grep_multi.search_terms %></pre>
</p>

<p>
  <strong>Search paths:</strong>
  <pre><%= @grep_multi.search_paths %></pre>
</p>

<%= link_to 'Edit', edit_grep_multi_path(@grep_multi) %> |
<%= link_to 'Back', grep_multis_path %>
